// Select (Headless) — 4주차 1단계
//
// 이 배럴은 로직(useSelect)만 내보낸다. 생김새(사이즈/번들/썸네일 리스트)는
// 하나로 고정하지 않고 사용처(src/app/products)가 각자 그린다 — 그게 Headless의 요지.
export { useSelect } from './useSelect'
export type { SelectOptionLike, OptionState } from './useSelect'
